import type { CoreVisualMode } from "../api/coreProductContracts";

type Vec3 = readonly [number, number, number];
type Rgb = readonly [number, number, number];
interface Mesh { positions:number[]; normals:number[]; colors:number[]; indices:number[]; }
export interface StadiumWebglRenderer { readonly triangleCount:number; resize(width:number,height:number,devicePixelRatio:number):void; render(orbitDegrees:number,zoom:number):void; destroy():void; }

const C={
  grass1:[0.035,0.285,0.115] as Rgb, grass2:[0.045,0.345,0.145] as Rgb, white:[0.91,0.94,0.92] as Rgb,
  seatDark:[0.105,0.135,0.145] as Rgb, seat:[0.16,0.205,0.215] as Rgb, seatLight:[0.23,0.285,0.29] as Rgb,
  concrete:[0.27,0.29,0.30] as Rgb, concreteDark:[0.12,0.135,0.14] as Rgb, roof:[0.39,0.42,0.43] as Rgb,
  steel:[0.34,0.37,0.38] as Rgb, black:[0.025,0.03,0.032] as Rgb, screen:[0.035,0.055,0.05] as Rgb,
  accent:[0.384,0.827,0.427] as Rgb, warm:[0.92,0.88,0.72] as Rgb,
} as const;
const mesh=():Mesh=>({positions:[],normals:[],colors:[],indices:[]});
const sub=(a:Vec3,b:Vec3):Vec3=>[a[0]-b[0],a[1]-b[1],a[2]-b[2]];
const cross=(a:Vec3,b:Vec3):Vec3=>[a[1]*b[2]-a[2]*b[1],a[2]*b[0]-a[0]*b[2],a[0]*b[1]-a[1]*b[0]];
const norm=(v:Vec3):Vec3=>{const l=Math.hypot(...v)||1;return[v[0]/l,v[1]/l,v[2]/l]};
function face(m:Mesh,a:Vec3,b:Vec3,c:Vec3,d:Vec3,color:Rgb){const n=norm(cross(sub(b,a),sub(c,a))),i=m.positions.length/3;for(const v of[a,b,c,d]){m.positions.push(...v);m.normals.push(...n);m.colors.push(...color)}m.indices.push(i,i+1,i+2,i,i+2,i+3)}
function box(m:Mesh,[x,y,z]:Vec3,[sx,sy,sz]:Vec3,c:Rgb){const x0=x-sx/2,x1=x+sx/2,y0=y-sy/2,y1=y+sy/2,z0=z-sz/2,z1=z+sz/2;face(m,[x0,y0,z1],[x1,y0,z1],[x1,y1,z1],[x0,y1,z1],c);face(m,[x1,y0,z0],[x0,y0,z0],[x0,y1,z0],[x1,y1,z0],c);face(m,[x0,y0,z0],[x0,y0,z1],[x0,y1,z1],[x0,y1,z0],c);face(m,[x1,y0,z1],[x1,y0,z0],[x1,y1,z0],[x1,y1,z1],c);face(m,[x0,y1,z1],[x1,y1,z1],[x1,y1,z0],[x0,y1,z0],c);face(m,[x0,y0,z0],[x1,y0,z0],[x1,y0,z1],[x0,y0,z1],c)}
function plane(m:Mesh,x0:number,x1:number,z0:number,z1:number,y:number,c:Rgb){face(m,[x0,y,z0],[x0,y,z1],[x1,y,z1],[x1,y,z0],c)}
function band(m:Mesh,ix:number,iz:number,ox:number,oz:number,iy:number,oy:number,seg:number,c1:Rgb,c2:Rgb){for(let i=0;i<seg;i++){const a=i/seg*Math.PI*2,b=(i+1)/seg*Math.PI*2,t=(i%8)/7,c:Rgb=[c1[0]+(c2[0]-c1[0])*t,c1[1]+(c2[1]-c1[1])*t,c1[2]+(c2[2]-c1[2])*t];face(m,[Math.cos(a)*ix,iy,Math.sin(a)*iz],[Math.cos(b)*ix,iy,Math.sin(b)*iz],[Math.cos(b)*ox,oy,Math.sin(b)*oz],[Math.cos(a)*ox,oy,Math.sin(a)*oz],c)}}
function wall(m:Mesh,rx:number,rz:number,y0:number,y1:number,seg:number,c:Rgb){for(let i=0;i<seg;i++){const a=i/seg*Math.PI*2,b=(i+1)/seg*Math.PI*2;face(m,[Math.cos(a)*rx,y0,Math.sin(a)*rz],[Math.cos(a)*rx,y1,Math.sin(a)*rz],[Math.cos(b)*rx,y1,Math.sin(b)*rz],[Math.cos(b)*rx,y0,Math.sin(b)*rz],c)}}
function circle(m:Mesh,r:number,w:number,y:number,seg:number,c:Rgb){for(let i=0;i<seg;i++){const a=i/seg*Math.PI*2,b=(i+1)/seg*Math.PI*2,ri=r-w/2,ro=r+w/2;face(m,[Math.cos(a)*ri,y,Math.sin(a)*ri],[Math.cos(b)*ri,y,Math.sin(b)*ri],[Math.cos(b)*ro,y,Math.sin(b)*ro],[Math.cos(a)*ro,y,Math.sin(a)*ro],c)}}
function pitch(m:Mesh,detail:number){plane(m,-62,62,-43,43,-.12,C.concreteDark);for(let i=0;i<12;i++){const x0=-52.5+i*8.75;plane(m,x0,x0+8.77,-34,34,.08,i%2?C.grass1:C.grass2)}const h=.16;box(m,[0,.18,-34],[105,h,.24],C.white);box(m,[0,.18,34],[105,h,.24],C.white);box(m,[-52.5,.18,0],[.24,h,68],C.white);box(m,[52.5,.18,0],[.24,h,68],C.white);box(m,[0,.18,0],[.24,h,68],C.white);circle(m,9.15,.24,.2,Math.max(40,detail/2),C.white);box(m,[0,.22,0],[.42,.12,.42],C.white);for(const s of[-1,1]){const gx=s*52.5,px=gx-s*16.5,ax=gx-s*5.5;box(m,[px,.18,0],[.24,h,40.32],C.white);box(m,[(gx+px)/2,.18,-20.16],[16.5,h,.24],C.white);box(m,[(gx+px)/2,.18,20.16],[16.5,h,.24],C.white);box(m,[ax,.18,0],[.24,h,18.32],C.white);box(m,[(gx+ax)/2,.18,-9.16],[5.5,h,.24],C.white);box(m,[(gx+ax)/2,.18,9.16],[5.5,h,.24],C.white);const post=gx+s*.25;box(m,[post,1.25,-3.66],[.18,2.5,.18],C.white);box(m,[post,1.25,3.66],[.18,2.5,.18],C.white);box(m,[post,2.5,0],[.18,.18,7.5],C.white);for(let j=-3;j<=3;j++)box(m,[gx+s*1.55,1.15,j*1.05],[.06,2.3,.06],[.65,.68,.67]);for(let j=0;j<5;j++)box(m,[gx+s*(.25+j*.33),1.15,-3.66],[.06,2.3,.06],[.65,.68,.67]);for(let j=0;j<5;j++)box(m,[gx+s*(.25+j*.33),1.15,3.66],[.06,2.3,.06],[.65,.68,.67])}}
function aisles(m:Mesh,rx0:number,rz0:number,rx1:number,rz1:number,y0:number,y1:number,count:number){for(let i=0;i<count;i++){const a=i/count*Math.PI*2,w=.008;const p=(r:number,z:number,t:number):Vec3=>[Math.cos(t)*r,0,Math.sin(t)*z];const a0=p(rx0,rz0,a-w),a1=p(rx0,rz0,a+w),b1=p(rx1,rz1,a+w),b0=p(rx1,rz1,a-w);face(m,[a0[0],y0,a0[2]],[a1[0],y0,a1[2]],[b1[0],y1,b1[2]],[b0[0],y1,b0[2]],C.concrete)} }
function rails(m:Mesh,rx:number,rz:number,y:number,count:number){for(let i=0;i<count;i++){const a=i/count*Math.PI*2;box(m,[Math.cos(a)*rx,y,Math.sin(a)*rz],[.12,1.15,.12],C.steel)}}
function canopy(m:Mesh,seg:number){band(m,91,65,112,82,34.5,39.5,seg,C.roof,C.steel);band(m,89,63,94,68,34.2,35.1,seg,C.black,C.roof);for(let i=0;i<32;i++){const a=i/32*Math.PI*2;const x=Math.cos(a),z=Math.sin(a);box(m,[x*102,36.8,z*74],[.45,5.5,.45],C.steel);box(m,[x*94,34,z*67],[.38,2.8,.38],C.steel)}}
function facade(m:Mesh,seg:number){wall(m,111,81,-1,8.5,seg,C.concreteDark);band(m,108,78,113,83,8.4,10.2,seg,C.concrete,C.concreteDark);for(let i=0;i<24;i++){const a=i/24*Math.PI*2;box(m,[Math.cos(a)*111,4.1,Math.sin(a)*81],[2.4,5.2,.7],i%3===0?C.black:C.concrete)} }
function benches(m:Mesh){for(const z of[-39.2,39.2]){box(m,[0,1.05,z],[18,1.8,2.2],C.concreteDark);box(m,[0,2.05,z],[18,.18,2.8],C.roof);for(let i=-7;i<=7;i+=2)box(m,[i,.75,z+(z>0?-1:1)*.3],[1.1,.5,.8],C.seatLight)}}
function screens(m:Mesh){box(m,[0,29,-79.5],[27,9,1.3],C.black);box(m,[0,29,-78.75],[23,6.2,.15],C.screen);box(m,[0,25.5,-78.6],[16,.45,.12],C.accent)}
function lights(m:Mesh){for(const x of[-101,101])for(const z of[-73,73]){box(m,[x,20,z],[.65,40,.65],C.steel);box(m,[x+(x>0?-2:2),40,z+(z>0?-1.2:1.2)],[8.5,2.7,1],C.concreteDark);for(let i=0;i<5;i++)for(let j=0;j<2;j++)box(m,[x+(x>0?-1:1)*(1+i*1.35),40.6-j*1.15,z+(z>0?-1:1)*1.7],[.9,.62,.22],C.warm)}}
function build(mode:Exclude<CoreVisualMode,"STATIC">):Mesh{const m=mesh(),seg=mode==="FULL"?112:mode==="FAST"?80:48;pitch(m,seg);facade(m,seg);band(m,62,44,76,55,1.2,10.5,seg,C.seatDark,C.seat);aisles(m,62,44,76,55,1.2,10.5,24);band(m,78,57,91,66,12,21.5,seg,C.seat,C.seatLight);aisles(m,78,57,91,66,12,21.5,28);band(m,93,68,106,77,23,33,seg,C.seatDark,C.seat);aisles(m,93,68,106,77,23,33,32);wall(m,61,43,.2,1.6,seg,C.concrete);wall(m,77,56,10.4,12,seg,C.concreteDark);wall(m,92,67,21.4,23,seg,C.concreteDark);rails(m,61.5,43.5,2.2,48);benches(m);screens(m);canopy(m,seg);lights(m);return m}
function perspective(f:number,a:number,n:number,far:number){const q=1/Math.tan(f/2),nf=1/(n-far);return new Float32Array([q/a,0,0,0,0,q,0,0,0,0,(far+n)*nf,-1,0,0,2*far*n*nf,0])}
const dot=(a:Vec3,b:Vec3)=>a[0]*b[0]+a[1]*b[1]+a[2]*b[2];
function lookAt(e:Vec3,t:Vec3,u:Vec3){const z=norm(sub(e,t)),x=norm(cross(u,z)),y=cross(z,x);return new Float32Array([x[0],y[0],z[0],0,x[1],y[1],z[1],0,x[2],y[2],z[2],0,-dot(x,e),-dot(y,e),-dot(z,e),1])}
function mul(a:Float32Array,b:Float32Array){const o=new Float32Array(16);for(let c=0;c<4;c++)for(let r=0;r<4;r++)o[c*4+r]=a[r]*b[c*4]+a[4+r]*b[c*4+1]+a[8+r]*b[c*4+2]+a[12+r]*b[c*4+3];return o}
function shader(gl:WebGLRenderingContext,type:number,src:string){const s=gl.createShader(type);if(!s)throw Error("STADIUM_SHADER_CREATE_FAILED");gl.shaderSource(s,src);gl.compileShader(s);if(!gl.getShaderParameter(s,gl.COMPILE_STATUS))throw Error(gl.getShaderInfoLog(s)||"shader");return s}
function program(gl:WebGLRenderingContext){const v=shader(gl,gl.VERTEX_SHADER,`attribute vec3 aPosition;attribute vec3 aNormal;attribute vec3 aColor;uniform mat4 uMvp;uniform vec3 uLightDirection;varying vec3 vColor;varying float vLight;varying float vHeight;void main(){vec3 n=normalize(aNormal);float d=max(dot(n,normalize(uLightDirection)),0.0);vLight=.42+d*.58;vColor=aColor;vHeight=aPosition.y;gl_Position=uMvp*vec4(aPosition,1.0);}`),f=shader(gl,gl.FRAGMENT_SHADER,`precision mediump float;varying vec3 vColor;varying float vLight;varying float vHeight;void main(){float haze=clamp((vHeight+4.0)/55.0,0.0,1.0);vec3 c=vColor*vLight;c+=vec3(.018,.022,.021)*haze;gl_FragColor=vec4(c,1.0);}`),p=gl.createProgram();if(!p)throw Error("STADIUM_PROGRAM_CREATE_FAILED");gl.attachShader(p,v);gl.attachShader(p,f);gl.linkProgram(p);gl.deleteShader(v);gl.deleteShader(f);if(!gl.getProgramParameter(p,gl.LINK_STATUS))throw Error(gl.getProgramInfoLog(p)||"link");return p}
function arr(gl:WebGLRenderingContext,d:number[]){const b=gl.createBuffer();if(!b)throw Error("STADIUM_BUFFER_CREATE_FAILED");gl.bindBuffer(gl.ARRAY_BUFFER,b);gl.bufferData(gl.