import type { CoreVisualMode } from "../api/coreProductContracts";

type Vec3 = readonly [number, number, number];
type Rgb = readonly [number, number, number];
interface Mesh { positions:number[]; normals:number[]; colors:number[]; indices:number[]; }
export interface StadiumWebglRenderer { readonly triangleCount:number; resize(width:number,height:number,devicePixelRatio:number):void; render(orbitDegrees:number,zoom:number):void; destroy():void; }

const C={grass1:[0.035,0.30,0.12] as Rgb,grass2:[0.055,0.39,0.16] as Rgb,white:[0.94,0.97,0.96] as Rgb,seatDark:[0.045,0.08,0.16] as Rgb,seat:[0.06,0.13,0.30] as Rgb,seatLight:[0.12,0.28,0.55] as Rgb,concrete:[0.30,0.32,0.34] as Rgb,concreteDark:[0.08,0.10,0.12] as Rgb,roof:[0.42,0.45,0.47] as Rgb,steel:[0.34,0.37,0.39] as Rgb,black:[0.015,0.02,0.025] as Rgb,screen:[0.02,0.05,0.11] as Rgb,accent:[0.12,0.42,1] as Rgb,warm:[1,0.94,0.72] as Rgb,crowdA:[0.73,0.73,0.74] as Rgb,crowdB:[0.20,0.38,0.75] as Rgb,crowdC:[0.10,0.15,0.23] as Rgb} as const;
const mesh=():Mesh=>({positions:[],normals:[],colors:[],indices:[]});
const sub=(a:Vec3,b:Vec3):Vec3=>[a[0]-b[0],a[1]-b[1],a[2]-b[2]];
const cross=(a:Vec3,b:Vec3):Vec3=>[a[1]*b[2]-a[2]*b[1],a[2]*b[0]-a[0]*b[2],a[0]*b[1]-a[1]*b[0]];
const norm=(v:Vec3):Vec3=>{const l=Math.hypot(...v)||1;return[v[0]/l,v[1]/l,v[2]/l]};
function face(m:Mesh,a:Vec3,b:Vec3,c:Vec3,d:Vec3,color:Rgb){const n=norm(cross(sub(b,a),sub(c,a))),i=m.positions.length/3;for(const v of[a,b,c,d]){m.positions.push(...v);m.normals.push(...n);m.colors.push(...color)}m.indices.push(i,i+1,i+2,i,i+2,i+3)}
function box(m:Mesh,[x,y,z]:Vec3,[sx,sy,sz]:Vec3,c:Rgb){const x0=x-sx/2,x1=x+sx/2,y0=y-sy/2,y1=y+sy/2,z0=z-sz/2,z1=z+sz/2;face(m,[x0,y0,z1],[x1,y0,z1],[x1,y1,z1],[x0,y1,z1],c);face(m,[x1,y0,z0],[x0,y0,z0],[x0,y1,z0],[x1,y1,z0],c);face(m,[x0,y0,z0],[x0,y0,z1],[x0,y1,z1],[x0,y1,z0],c);face(m,[x1,y0,z1],[x1,y0,z0],[x1,y1,z0],[x1,y1,z1],c);face(m,[x0,y1,z1],[x1,y1,z1],[x1,y1,z0],[x0,y1,z0],c);face(m,[x0,y0,z0],[x1,y0,z0],[x1,y0,z1],[x0,y0,z1],c)}
function plane(m:Mesh,x0:number,x1:number,z0:number,z1:number,y:number,c:Rgb){face(m,[x0,y,z0],[x0,y,z1],[x1,y,z1],[x1,y,z0],c)}
function band(m:Mesh,ix:number,iz:number,ox:number,oz:number,iy:number,oy:number,seg:number,c1:Rgb,c2:Rgb){for(let i=0;i<seg;i++){const a=i/seg*Math.PI*2,b=(i+1)/seg*Math.PI*2,t=(i%10)/9,c:Rgb=[c1[0]+(c2[0]-c1[0])*t,c1[1]+(c2[1]-c1[1])*t,c1[2]+(c2[2]-c1[2])*t];face(m,[Math.cos(a)*ix,iy,Math.sin(a)*iz],[Math.cos(b)*ix,iy,Math.sin(b)*iz],[Math.cos(b)*ox,oy,Math.sin(b)*oz],[Math.cos(a)*ox,oy,Math.sin(a)*oz],c)}}
function wall(m:Mesh,rx:number,rz:number,y0:number,y1:number,seg:number,c:Rgb){for(let i=0;i<seg;i++){const a=i/seg*Math.PI*2,b=(i+1)/seg*Math.PI*2;face(m,[Math.cos(a)*rx,y0,Math.sin(a)*rz],[Math.cos(a)*rx,y1,Math.sin(a)*rz],[Math.cos(b)*rx,y1,Math.sin(b)*rz],[Math.cos(b)*rx,y0,Math.sin(b)*rz],c)}}
function circle(m:Mesh,r:number,w:number,y:number,seg:number,c:Rgb){for(let i=0;i<seg;i++){const a=i/seg*Math.PI*2,b=(i+1)/seg*Math.PI*2,ri=r-w/2,ro=r+w/2;face(m,[Math.cos(a)*ri,y,Math.sin(a)*ri],[Math.cos(b)*ri,y,Math.sin(b)*ri],[Math.cos(b)*ro,y,Math.sin(b)*ro],[Math.cos(a)*ro,y,Math.sin(a)*ro],c)}}
function pitch(m:Mesh,detail:number){plane(m,-64,64,-45,45,-.2,C.concreteDark);for(let i=0;i<12;i++){const x0=-52.5+i*8.75;plane(m,x0,x0+8.77,-34,34,.08,i%2?C.grass1:C.grass2)}const h=.16;for(const [c,s] of [[[0,.18,-34],[105,h,.24]],[[0,.18,34],[105,h,.24]],[[-52.5,.18,0],[.24,h,68]],[[52.5,.18,0],[.24,h,68]],[[0,.18,0],[.24,h,68]]] as const)box(m,c,s,C.white);circle(m,9.15,.24,.2,Math.max(40,detail/2),C.white);box(m,[0,.22,0],[.42,.12,.42],C.white);for(const s of[-1,1]){const gx=s*52.5,px=gx-s*16.5,ax=gx-s*5.5;box(m,[px,.18,0],[.24,h,40.32],C.white);box(m,[(gx+px)/2,.18,-20.16],[16.5,h,.24],C.white);box(m,[(gx+px)/2,.18,20.16],[16.5,h,.24],C.white);box(m,[ax,.18,0],[.24,h,18.32],C.white);box(m,[(gx+ax)/2,.18,-9.16],[5.5,h,.24],C.white);box(m,[(gx+ax)/2,.18,9.16],[5.5,h,.24],C.white);const post=gx+s*.25;box(m,[post,1.25,-3.66],[.18,2.5,.18],C.white);box(m,[post,1.25,3.66],[.18,2.5,.18],C.white);box(m,[post,2.5,0],[.18,.18,7.5],C.white);for(let j=-3;j<=3;j++)box(m,[gx+s*1.55,1.15,j*1.05],[.05,2.3,.05],[.67,.70,.70]);for(let j=0;j<5;j++){box(m,[gx+s*(.25+j*.33),1.15,-3.66],[.05,2.3,.05],[.67,.70,.70]);box(m,[gx+s*(.25+j*.33),1.15,3.66],[.05,2.3,.05],[.67,.70,.70])}}}
function aisles(m:Mesh,rx0:number,rz0:number,rx1:number,rz1:number,y0:number,y1:number,count:number){for(let i=0;i<count;i++){const a=i/count*Math.PI*2,w=.008;face(m,[Math.cos(a-w)*rx0,y0,Math.sin(a-w)*rz0],[Math.cos(a+w)*rx0,y0,Math.sin(a+w)*rz0],[Math.cos(a+w)*rx1,y1,Math.sin(a+w)*rz1],[Math.cos(a-w)*rx1,y1,Math.sin(a-w)*rz1],C.concrete)}}
function crowd(m:Mesh,rx0:number,rz0:number,rx1:number,rz1:number,y0:number,y1:number,rows:number,seats:number,mode:Exclude<CoreVisualMode,"STATIC">){const rowStep=mode==="FULL"?1:2,seatStep=mode==="LIGHT"?4:2;for(let r=0;r<rows;r+=rowStep){const t=(r+.5)/rows,rx=rx0+(rx1-rx0)*t,rz=rz0+(rz1-rz0)*t,y=y0+(y1-y0)*t+.25;for(let i=0;i<seats;i+=seatStep){if((i+r*3)%17===0)continue;const a=(i+.5)/seats*Math.PI*2;const pick=(i+r)%9,c=pick<2?C.crowdA:pick<6?C.crowdB:C.crowdC;box(m,[Math.cos(a)*rx,y,Math.sin(a)*rz],[.42,.68,.42],c)}}}
function rails(m:Mesh,rx:number,rz:number,y:number,count:number){for(let i=0;i<count;i++){const a=i/count*Math.PI*2;box(m,[Math.cos(a)*rx,y,Math.sin(a)*rz],[.12,1.15,.12],C.steel)}}
function ledRibbon(m:Mesh,rx:number,rz:number,y:number,seg:number){for(let i=0;i<seg;i++){const a=i/seg*Math.PI*2,b=(i+1)/seg*Math.PI*2;face(m,[Math.cos(a)*rx,y,Math.sin(a)*rz],[Math.cos(a)*rx,y+.7,Math.sin(a)*rz],[Math.cos(b)*rx,y+.7,Math.sin(b)*rz],[Math.cos(b)*rx,y,Math.sin(b)*rz],i%5===0?C.white:C.accent)}}
function canopy(m:Mesh,seg:number){band(m,89,63,113,83,34.4,40.5,seg,C.roof,C.steel);band(m,88,62,94,68,34.0,35.1,seg,C.black,C.roof);for(let i=0;i<40;i++){const a=i/40*Math.PI*2,x=Math.cos(a),z=Math.sin(a);box(m,[x*102,37.1,z*74],[.45,6.4,.45],C.steel);box(m,[x*95,34.7,z*68],[.34,3.6,.34],C.steel);box(m,[x*108,39.1,z*79],[.30,3.0,.30],C.steel)}}
function facade(m:Mesh,seg:number){wall(m,112,82,-1,9,seg,C.concreteDark);band(m,108,78,114,84,8.7,10.5,seg,C.concrete,C.concreteDark);for(let i=0;i<32;i++){const a=i/32*Math.PI*2;box(m,[Math.cos(a)*111,4.1,Math.sin(a)*81],[2.0,5.4,.65],i%4===0?C.black:C.concrete)}}
function benches(m:Mesh){for(const z of[-39.2,39.2]){box(m,[0,1.0,z],[18,1.8,2.2],C.concreteDark);box(m,[0,2.05,z],[18,.18,2.8],C.roof);for(let i=-7;i<=7;i+=2)box(m,[i,.75,z+(z>0?-1:1)*.3],[1.1,.5,.8],C.seatLight)}}
function tunnel(m:Mesh){box(m,[0,1.9,44.3],[9,3.8,5.6],C.black);box(m,[0,4.0,44.3],[11,.35,6.0],C.accent);for(const x of[-5.8,5.8])box(m,[x,2.0,43.8],[.7,4.0,.7],C.steel)}
function screens(m:Mesh){box(m,[0,29,-79.5],[30,10.5,1.3],C.black);box(m,[0,29,-78.75],[25,7.0,.15],C.screen);box(m,[0,25.2,-78.6],[18,.48,.12],C.accent);for(const x of[-70,70]){box(m,[x,22,-57],[12,7,1],C.black);box(m,[x,22,-56.5],[10,5.2,.15],C.screen)}}
function lights(m:Mesh){for(const x of[-101,101])for(const z of[-73,73]){box(m,[x,20,z],[.65,40,.65],C.steel);box(m,[x+(x>0?-2:2),40,z+(z>0?-1.2:1.2)],[9.5,3.2,1],C.concreteDark);for(let i=0;i<6;i++)for(let j=0;j<3;j++)box(m,[x+(x>0?-1:1)*(1+i*1.28),41-j*.95,z+(z>0?-1:1)*1.7],[.9,.62,.22],C.warm)}}
function build(mode:Exclude<CoreVisualMode,"STATIC">):Mesh{const m=mesh(),seg=mode==="FULL"?128:mode==="FAST"?88:52;pitch(m,seg);facade(m,seg);band(m,61,43,77,56,1.0,10.8,seg,C.seatDark,C.seat);aisles(m,61,43,77,56,1.0,10.8,28);crowd(m,62,44,76,55,1.5,10.5,12,180,mode);band(m,78,57,92,67,12,22,seg,C.seat,C.seatLight);aisles(m,78,57,92,67,12,22,32);crowd(m,79,58,91,66,12.5,21.7,12,210,mode);band(m,93,68,107,78,23,33.5,seg,C.seatDark,C.seat);aisles(m,93,68,107,78,23,33.5,36);crowd(m,94,69,106,77,23.5,33.1,13,230,mode);wall(m,60.5,42.5,.2,1.5,seg,C.concrete);wall(m,77,56,10.6,12,seg,C.concreteDark);wall(m,92,67,21.6,23,seg,C.concreteDark);ledRibbon(m,77.5,56.5,11.45,seg);ledRibbon(m,92.5,67.5,22.3,seg);rails(m,61.5,43.5,2.2,56);benches(m);tunnel(m);screens(m);canopy(m,seg);lights(m);return m}
function perspective(f:number,a:number,n:number,far:number){const q=1/Math.tan(f/2),nf=1/(n-far);return new Float32Array([q/a,0,0,0,0,q,0,0,0,0,(far+n)*nf,-1,0,0,2*far*n*nf,0])}
const dot=(a:Vec3,b:Vec3)=>a[0]*b[0]+a[1]*b[1]+a[2]*b[2];
function lookAt(e:Vec3,t:Vec3,u:Vec3){const z=norm(sub(e,t)),x=norm(cross(u,z)),y=cross(z,x);return new Float32Array([x[0],y[0],z[0],0,x[1],y[1],z[1],0,x[2],y[2],z[2],0,-dot(x,e),-dot(y,e),-dot(z,e),1])}
function mul(a:Float32Array,b:Float32Array){const o=new Float32Array(16);for(let c=0;c<4;c++)for(let r=0;r<4;r++)o[c*4+r]=a[r]*b[c*4]+a[4+r]*b[c*4+1]+a[8+r]*b[c*4+2]+a[12+r]*b[c*4+3];return o}
function shader(gl:WebGLRenderingContext,type:number,src:string){const s=gl.createShader(type);if(!s)throw Error("STADIUM_SHADER_CREATE_FAILED");gl.shaderSource(s,src);gl.compileShader(s);if(!gl.getShaderParameter(s,gl.COMPILE_STATUS)){const msg=gl.getShaderInfoLog(s)||"shader";gl.deleteShader(s);throw Error(msg)}return s}
function program(gl:WebGLRenderingContext){const v=shader(gl,gl.VERTEX_SHADER,`attribute vec3 aPosition;attribute vec3 aNormal;attribute vec3 aColor;uniform mat4 uMvp;uniform vec3 uLightDirection;varying vec3 vColor;varying float vLight;varying float vHeight;void main(){vec3 n=normalize(aNormal);float d=max(dot(n,normalize(uLightDirection)),0.0);vLight=.34+d*.66;vColor=aColor;vHeight=aPosition.y;gl_Position=uMvp*vec4(aPosition,1.0);}`),f=shader(gl,gl.FRAGMENT_SHADER,`precision mediump float;varying vec3 vColor;varying float vLight;varying float vHeight;void main(){float h=clamp((vHeight+2.0)/50.0,0.0,1.0);vec3 c=vColor*vLight;c+=vec3(.018,.025,.045)*h;gl_FragColor=vec4(c,1.0);}`),p=gl.createProgram();if(!p)throw Error("STADIUM_PROGRAM_CREATE_FAILED");gl.attachShader(p,v);gl.attachShader(p,f);gl.linkProgram(p);gl.deleteShader(v);gl.deleteShader(f);if(!gl.getProgramParameter(p,gl.LINK_STATUS))throw Error(gl.getProgramInfoLog(p)||"link");return p}
function arr(gl:WebGLRenderingContext,d:number[]){const b=gl.createBuffer();if(!b)throw Error("STADIUM_BUFFER_CREATE_FAILED");gl.bindBuffer(gl.ARRAY_BUFFER,b);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array(d),gl.STATIC_DRAW);return b}
export function createStadiumWebglRenderer(canvas:HTMLCanvasElement,mode:Exclude<CoreVisualMode,"STATIC">):StadiumWebglRenderer|null{if(typeof window.WebGLRenderingContext==="undefined")return null;const gl=canvas.getContext("webgl",{alpha:true,antialias:true,depth:true,premultipliedAlpha:false,powerPreference:mode==="FULL"?"high-performance":"default"});if(!gl)return null;const m=build(mode),p=program(gl),pb=arr(gl,m.positions),nb=arr(gl,m.normals),cb=arr(gl,m.colors),ib=gl.createBuffer();if(!ib)throw Error("STADIUM_INDEX_BUFFER_CREATE_FAILED");gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER,ib);const max=m.indices.reduce((a,b)=>Math.max(a,b),0),u32=max>65535;if(u32&&!gl.getExtension("OES_element_index_uint"))throw Error("STADIUM_INDEX_RANGE_UNSUPPORTED");gl.bufferData(gl.ELEMENT_ARRAY_BUFFER,u32?new Uint32Array(m.indices):new Uint16Array(m.indices),gl.STATIC_DRAW);const pl=gl.getAttribLocation(p,"aPosition"),nl=gl.getAttribLocation(p,"aNormal"),cl=gl.getAttribLocation(p,"aColor"),ml=gl.getUniformLocation(p,"uMvp"),ll=gl.getUniformLocation(p,"uLightDirection");if(pl<0||nl<0||cl<0||!ml||!ll)throw Error("STADIUM_SHADER_LOCATION_FAILED");let vw=1,vh=1;gl.enable(gl.DEPTH_TEST);gl.depthFunc(gl.LEQUAL);gl.disable(gl.CULL_FACE);const bind=(b:WebGLBuffer,l:number)=>{gl.bindBuffer(gl.ARRAY_BUFFER,b);gl.enableVertexAttribArray(l);gl.vertexAttribPointer(l,3,gl.FLOAT,false,0,0)};return{triangleCount:m.indices.length/3,resize(w,h,dpr){vw=Math.max(1,Math.round(w*Math.min(dpr,2)));vh=Math.max(1,Math.round(h*Math.min(dpr,2)));canvas.width=vw;canvas.height=vh;gl.viewport(0,0,vw,vh)},render(orbit,zoom){const a=orbit*Math.PI/180,z=Math.min(1.16,Math.max(.92,zoom)),aspect=vw/vh,portrait=aspect<1,r=(portrait?292:222)/z,eh=(portrait?145:112)/z,e:[number,number,number]=[Math.sin(a)*r,eh,Math.cos(a)*r],view=lookAt(e,[0,11,0],[0,1,0]),proj=perspective((portrait?60:45)*Math.PI/180,aspect,1,650),mvp=mul(proj,view);gl.clearColor(0,0,0,0);gl.clearDepth(1);gl.clear(gl.COLOR_BUFFER_BIT|gl.DEPTH_BUFFER_BIT);gl.useProgram(p);bind(pb,pl);bind(nb,nl);bind(cb,cl);gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER,ib);gl.uniformMatrix4fv(ml,false,mvp);gl.uniform3f(ll,-.28,.92,.52);gl.drawElements(gl.TRIANGLES,m.indices.length,u32?gl.UNSIGNED_INT:gl.UNSIGNED_SHORT,0)},destroy(){gl.deleteBuffer(pb);gl.deleteBuffer(nb);gl.deleteBuffer(cb);gl.deleteBuffer(ib);gl.deleteProgram(p)}}}
