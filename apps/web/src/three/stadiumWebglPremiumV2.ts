/* eslint-disable no-undef */
import type { CoreVisualMode } from "../api/coreProductContracts";

type Vec3=readonly[number,number,number];
type Rgb=readonly[number,number,number];
interface Mesh{positions:number[];normals:number[];colors:number[];indices:number[]}
export interface StadiumWebglRenderer{readonly triangleCount:number;resize(width:number,height:number,devicePixelRatio:number):void;render(orbitDegrees:number,zoom:number):void;destroy():void}

const C={
 grassA:[.035,.22,.055]as Rgb,grassB:[.025,.16,.038]as Rgb,white:[.86,.89,.90]as Rgb,
 seat0:[.010,.018,.038]as Rgb,seat1:[.018,.055,.15]as Rgb,seat2:[.025,.11,.30]as Rgb,
 concrete:[.13,.145,.16]as Rgb,dark:[.018,.023,.030]as Rgb,roof:[.16,.18,.205]as Rgb,
 steel:[.29,.32,.35]as Rgb,black:[.002,.003,.005]as Rgb,led:[.025,.20,.78]as Rgb,
 ledWhite:[.74,.86,1]as Rgb,warm:[1,.76,.40]as Rgb,crowd:[.20,.26,.34]as Rgb,
 crowdBlue:[.06,.16,.34]as Rgb,crowdWhite:[.56,.60,.64]as Rgb
}as const;
const mesh=():Mesh=>({positions:[],normals:[],colors:[],indices:[]});
const sub=(a:Vec3,b:Vec3):Vec3=>[a[0]-b[0],a[1]-b[1],a[2]-b[2]];
const cross=(a:Vec3,b:Vec3):Vec3=>[a[1]*b[2]-a[2]*b[1],a[2]*b[0]-a[0]*b[2],a[0]*b[1]-a[1]*b[0]];
const norm=(v:Vec3):Vec3=>{const l=Math.hypot(v[0],v[1],v[2])||1;return[v[0]/l,v[1]/l,v[2]/l]};
function face(m:Mesh,a:Vec3,b:Vec3,c:Vec3,d:Vec3,col:Rgb){const n=norm(cross(sub(b,a),sub(c,a))),i=m.positions.length/3;for(const v of[a,b,c,d]){m.positions.push(...v);m.normals.push(...n);m.colors.push(...col)}m.indices.push(i,i+1,i+2,i,i+2,i+3)}
function box(m:Mesh,[x,y,z]:Vec3,[sx,sy,sz]:Vec3,c:Rgb){const x0=x-sx/2,x1=x+sx/2,y0=y-sy/2,y1=y+sy/2,z0=z-sz/2,z1=z+sz/2;face(m,[x0,y0,z1],[x1,y0,z1],[x1,y1,z1],[x0,y1,z1],c);face(m,[x1,y0,z0],[x0,y0,z0],[x0,y1,z0],[x1,y1,z0],c);face(m,[x0,y0,z0],[x0,y0,z1],[x0,y1,z1],[x0,y1,z0],c);face(m,[x1,y0,z1],[x1,y0,z0],[x1,y1,z0],[x1,y1,z1],c);face(m,[x0,y1,z1],[x1,y1,z1],[x1,y1,z0],[x0,y1,z0],c);face(m,[x0,y0,z0],[x1,y0,z0],[x1,y0,z1],[x0,y0,z1],c)}
function plane(m:Mesh,x0:number,x1:number,z0:number,z1:number,y:number,c:Rgb){face(m,[x0,y,z0],[x0,y,z1],[x1,y,z1],[x1,y,z0],c)}
function band(m:Mesh,ix:number,iz:number,ox:number,oz:number,iy:number,oy:number,seg:number,c:Rgb,cut=9){for(let i=0;i<seg;i++){const a=i/seg*Math.PI*2,b=(i+1)/seg*Math.PI*2,mid=(a+b)/2;if(Math.sin(mid)>cut)continue;face(m,[Math.cos(a)*ix,iy,Math.sin(a)*iz],[Math.cos(b)*ix,iy,Math.sin(b)*iz],[Math.cos(b)*ox,oy,Math.sin(b)*oz],[Math.cos(a)*ox,oy,Math.sin(a)*oz],c)}}
function wall(m:Mesh,rx:number,rz:number,y0:number,y1:number,seg:number,c:Rgb){for(let i=0;i<seg;i++){const a=i/seg*Math.PI*2,b=(i+1)/seg*Math.PI*2;face(m,[Math.cos(a)*rx,y0,Math.sin(a)*rz],[Math.cos(a)*rx,y1,Math.sin(a)*rz],[Math.cos(b)*rx,y1,Math.sin(b)*rz],[Math.cos(b)*rx,y0,Math.sin(b)*rz],c)}}
function circle(m:Mesh,r:number,w:number,y:number,seg:number,c:Rgb){for(let i=0;i<seg;i++){const a=i/seg*Math.PI*2,b=(i+1)/seg*Math.PI*2,ri=r-w/2,ro=r+w/2;face(m,[Math.cos(a)*ri,y,Math.sin(a)*ri],[Math.cos(b)*ri,y,Math.sin(b)*ri],[Math.cos(b)*ro,y,Math.sin(b)*ro],[Math.cos(a)*ro,y,Math.sin(a)*ro],c)}}
function pitch(m:Mesh,detail:number){plane(m,-62,62,-43,43,-.18,C.dark);for(let i=0;i<14;i++){const x=-52.5+i*7.5;plane(m,x,x+7.51,-34,34,.08,i%2?C.grassA:C.grassB)}const h=.08;for(const [p,s]of[[[0,.18,-34],[105,h,.18]],[[0,.18,34],[105,h,.18]],[[-52.5,.18,0],[.18,h,68]],[[52.5,.18,0],[.18,h,68]],[[0,.18,0],[.18,h,68]]]as const)box(m,p,s,C.white);circle(m,9.15,.18,.20,Math.max(64,detail>>1),C.white);box(m,[0,.22,0],[.28,.08,.28],C.white);for(const side of[-1,1]as const){const gx=side*52.5,px=gx-side*16.5,ax=gx-side*5.5;box(m,[px,.18,0],[.18,h,40.32],C.white);box(m,[(gx+px)/2,.18,-20.16],[16.5,h,.18],C.white);box(m,[(gx+px)/2,.18,20.16],[16.5,h,.18],C.white);box(m,[ax,.18,0],[.18,h,18.32],C.white);box(m,[(gx+ax)/2,.18,-9.16],[5.5,h,.18],C.white);box(m,[(gx+ax)/2,.18,9.16],[5.5,h,.18],C.white);const post=gx+side*.20;box(m,[post,1.25,-3.66],[.13,2.5,.13],C.white);box(m,[post,1.25,3.66],[.13,2.5,.13],C.white);box(m,[post,2.5,0],[.13,.13,7.45],C.white);for(let j=-5;j<=5;j++)box(m,[gx+side*1.25,1.05,j*.7],[.022,2.0,.022],C.crowdWhite);for(let j=0;j<7;j++){box(m,[gx+side*(.22+j*.18),1.05,-3.66],[.022,2.0,.022],C.crowdWhite);box(m,[gx+side*(.22+j*.18),1.05,3.66],[.022,2.0,.022],C.crowdWhite)}}for(const z of[-38.8,38.8]){box(m,[0,.62,z],[22,1.12,2.1],C.dark);box(m,[0,1.55,z],[22,.14,2.7],C.steel);for(let x=-8;x<=8;x+=2)box(m,[x,.46,z+(z>0?-1:1)*.34],[1.05,.38,.72],C.seat2)}}
interface Tier{ix:number;iz:number;rows:number;depth:number;rise:number;y:number;seats:number;c0:Rgb;c1:Rgb}
function tier(m:Mesh,t:Tier,seg:number,lightMode:boolean){const seats=lightMode?Math.floor(t.seats*.55):t.seats;for(let r=0;r<t.rows;r++){const u=r/Math.max(1,t.rows-1),ix=t.ix+r*t.depth,iz=t.iz+r*t.depth*.72,ox=ix+t.depth*.96,oz=iz+t.depth*.69,y=t.y+r*t.rise,c:Rgb=[t.c0[0]+(t.c1[0]-t.c0[0])*u,t.c0[1]+(t.c1[1]-t.c0[1])*u,t.c0[2]+(t.c1[2]-t.c0[2])*u];band(m,ix,iz,ox,oz,y,y+t.rise*.10,seg,c);if(r%2===0)wall(m,ox,oz,y-.20,y+.10,seg,C.dark);for(let i=0;i<seats;i++){const h=(i*37+r*71)%101;if(h<18)continue;const a=(i+.5)/seats*Math.PI*2,rx=ix+t.depth*.53,rz=iz+t.depth*.38;let cc:Rgb=C.seat1;if(h>94)cc=C.crowdWhite;else if(h>80)cc=C.crowd;else if(h>66)cc=C.crowdBlue;else if(h>45)cc=C.seat2;box(m,[Math.cos(a)*rx,y+.34+(h%5)*.018,Math.sin(a)*rz],[.14,.36+(h%4)*.025,.14],cc)}}}
function aisles(m:Mesh,ix:number,iz:number,ox:number,oz:number,y0:number,y1:number,count:number){for(let i=0;i<count;i++){const a=i/count*Math.PI*2,w=.006;face(m,[Math.cos(a-w)*ix,y0,Math.sin(a-w)*iz],[Math.cos(a+w)*ix,y0,Math.sin(a+w)*iz],[Math.cos(a+w)*ox,y1,Math.sin(a+w)*oz],[Math.cos(a-w)*ox,y1,Math.sin(a-w)*oz],C.concrete)}}
function ribbon(m:Mesh,rx:number,rz:number,y:number,seg:number){for(let i=0;i<seg;i++){const a=i/seg*Math.PI*2,b=(i+1)/seg*Math.PI*2,c=i%17===0?C.ledWhite:C.led;face(m,[Math.cos(a)*rx,y,Math.sin(a)*rz],[Math.cos(a)*rx,y+.42,Math.sin(a)*rz],[Math.cos(b)*rx,y+.42,Math.sin(b)*rz],[Math.cos(b)*rx,y,Math.sin(b)*rz],c)}}
function facade(m:Mesh,seg:number){wall(m,113,83,-1.4,5.6,seg,C.dark);band(m,109,79,115,85,5.2,7,seg,C.concrete);for(let i=0;i<56;i++){const a=i/56*Math.PI*2;box(m,[Math.cos(a)*111.8,2.5,Math.sin(a)*82],[1.15,3,.24],i%6===0?C.warm:C.black)}box(m,[0,1.6,82],[22,3.2,2],C.black);box(m,[0,4.4,81.4],[24,.28,1],C.led)}
function roof(m:Mesh,seg:number){band(m,94,68,117,86,38.2,43.2,seg,C.roof,.78);band(m,91,65,94,68,37.7,38.5,seg,C.black,.78);for(let i=0;i<52;i++){const a=i/52*Math.PI*2;if(Math.sin(a)>.80)continue;const x=Math.cos(a),z=Math.sin(a);box(m,[x*106,40.9,z*78],[.32,5.4,.32],C.steel);box(m,[x*97,38.4,z*70],[.24,2,.24],C.steel)}}
function screens(m:Mesh){box(m,[0,31.5,-80],[32,12,1],C.black);box(m,[0,31.5,-79.35],[28,8.4,.08],[.008,.025,.07]);box(m,[0,27.4,-79.2],[22,.38,.08],C.led);for(const x of[-58,58]){box(m,[x,26,-67],[14,7,.8],C.black);box(m,[x,26,-66.5],[12,5,.06],[.008,.025,.07])}}
function lights(m:Mesh){for(const x of[-104,104])for(const z of[-75,75]){box(m,[x,22,z],[.6,44,.6],C.steel);box(m,[x+(x>0?-2.4:2.4),44,z+(z>0?-1.6:1.6)],[11.5,3.9,1.1],C.dark);for(let c=0;c<8;c++)for(let r=0;r<3;r++)box(m,[x+(x>0?-1:1)*(1+c*1.25),45.2-r,z+(z>0?-1:1)*1.8],[.9,.62,.22],C.warm)}}
function build(mode:Exclude<CoreVisualMode,"STATIC">){const m=mesh(),seg=mode==="FULL"?160:mode==="FAST"?120:80,light=mode==="LIGHT";pitch(m,seg);facade(m,seg);tier(m,{ix:61,iz:43,rows:13,depth:1.22,rise:.70,y:1,seats:104,c0:C.seat0,c1:C.seat1},seg,light);aisles(m,61,43,77,55,1,9.5,32);ribbon(m,77.5,55.5,10,seg);tier(m,{ix:79,iz:57,rows:13,depth:1.10,rise:.74,y:11,seats:116,c0:C.seat0,c1:C.seat2},seg,light);aisles(m,79,57,93,67,11,20.5,36);ribbon(m,93.5,67.5,21,seg);tier(m,{ix:95,iz:69,rows:12,depth:1.08,rise:.76,y:22,seats:124,c0:C.seat0,c1:C.seat1},seg,light);aisles(m,95,69,108,78,22,31,40);screens(m);roof(m,seg);lights(m);return m}
function persp(f:number,a:number,n:number,far:number){const q=1/Math.tan(f/2),nf=1/(n-far);return new Float32Array([q/a,0,0,0,0,q,0,0,0,0,(far+n)*nf,-1,0,0,2*far*n*nf,0])}
const dot=(a:Vec3,b:Vec3)=>a[0]*b[0]+a[1]*b[1]+a[2]*b[2];
function look(e:Vec3,t:Vec3,u:Vec3){const z=norm(sub(e,t)),x=norm(cross(u,z)),y=cross(z,x);return new Float32Array([x[0],y[0],z[0],0,x[1],y[1],z[1],0,x[2],y[2],z[2],0,-dot(x,e),-dot(y,e),-dot(z,e),1])}
function mul(a:Float32Array,b:Float32Array){const o=new Float32Array(16);for(let c=0;c<4;c++)for(let r=0;r<4;r++)o[c*4+r]=a[r]*b[c*4]+a[4+r]*b[c*4+1]+a[8+r]*b[c*4+2]+a[12+r]*b[c*4+3];return o}
function shader(gl:WebGLRenderingContext,type:number,src:string){const s=gl.createShader(type);if(!s)throw Error("STADIUM_SHADER_CREATE_FAILED");gl.shaderSource(s,src);gl.compileShader(s);if(!gl.getShaderParameter(s,gl.COMPILE_STATUS))throw Error(gl.getShaderInfoLog(s)||"shader");return s}
function program(gl:WebGLRenderingContext){const vs=shader(gl,gl.VERTEX_SHADER,`attribute vec3 aPosition;attribute vec3 aNormal;attribute vec3 aColor;uniform mat4 uMvp;varying vec3 vPos;varying vec3 vNormal;varying vec3 vColor;void main(){vPos=aPosition;vNormal=normalize(aNormal);vColor=aColor;gl_Position=uMvp*vec4(aPosition,1.0);}`),fs=shader(gl,gl.FRAGMENT_SHADER,`precision mediump float;varying vec3 vPos;varying vec3 vNormal;varying vec3 vColor;uniform vec3 uCamera;uniform vec3 uSunDir;vec3 lamp(vec3 lp,float power){vec3 toP=vPos-lp;float dist=max(length(toP),1.0);vec3 L=normalize(-toP);vec3 axis=normalize(-lp);float cone=smoothstep(.62,.91,dot(normalize(toP),axis));float nd=max(dot(vNormal,L),0.0);float att=1.0/(1.0+.00032*dist*dist);vec3 V=normalize(uCamera-vPos);vec3 H=normalize(L+V);float mx=max(max(vColor.r,vColor.g),vColor.b);float specW=mix(.035,.18,smoothstep(.55,.95,mx));float spec=pow(max(dot(vNormal,H),0.0),38.0)*specW;return vec3(1.0,.94,.82)*(nd+spec)*att*power*(.38+.92*cone);}void main(){float onPitch=(1.0-step(53.0,abs(vPos.x)))*(1.0-step(35.0,abs(vPos.z)))*(1.0-step(.7,vPos.y));float grain=(sin(vPos.x*2.7)+sin(vPos.z*4.1)+sin((vPos.x+vPos.z)*1.3))*.012;vec3 base=vColor*(1.0+grain*onPitch);float up=max(vNormal.y,0.0);float sun=max(dot(vNormal,normalize(uSunDir)),0.0);float ao=mix(.60,1.0,smoothstep(0.0,32.0,vPos.y));ao=mix(ao,1.0,onPitch);vec3 c=base*(vec3(.045,.052,.060)*(ao+.28*up)+sun*.14);c+=base*lamp(vec3(-104.0,45.0,-75.0),3.2);c+=base*lamp(vec3(104.0,45.0,-75.0),3.2);c+=base*lamp(vec3(-104.0,45.0,75.0),2.9);c+=base*lamp(vec3(104.0,45.0,75.0),2.9);c+=base*vec3(.24,.22,.17)*onPitch*.48;float mx=max(max(base.r,base.g),base.b);float emiss=smoothstep(.91,.99,mx);c+=base*emiss*1.25;float rim=pow(1.0-max(dot(normalize(uCamera-vPos),vNormal),0.0),3.0);c+=vec3(.055,.075,.105)*rim*.16;float dist=length(uCamera-vPos);float fog=smoothstep(220.0,440.0,dist);c=mix(c,vec3(.012,.017,.023),fog*.28);c=c/(c+vec3(.84));c=pow(c,vec3(.82));gl_FragColor=vec4(c,1.0);}`),p=gl.createProgram();if(!p)throw Error("STADIUM_PROGRAM_CREATE_FAILED");gl.attachShader(p,vs);gl.attachShader(p,fs);gl.linkProgram(p);gl.deleteShader(vs);gl.deleteShader(fs);if(!gl.getProgramParameter(p,gl.LINK_STATUS))throw Error(gl.getProgramInfoLog(p)||"link");return p}
function arr(gl:WebGLRenderingContext,d:number[]){const b=gl.createBuffer();if(!b)throw Error("STADIUM_BUFFER_CREATE_FAILED");gl.bindBuffer(gl.ARRAY_BUFFER,b);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array(d),gl.STATIC_DRAW);return b}
export function createStadiumWebglRenderer(canvas:HTMLCanvasElement,mode:Exclude<CoreVisualMode,"STATIC">):StadiumWebglRenderer|null{if(typeof window.WebGLRenderingContext==="undefined")return null;const maybe=canvas.getContext("webgl",{alpha:true,antialias:true,depth:true,premultipliedAlpha:false,powerPreference:mode==="FULL"?"high-performance":"default"})as WebGLRenderingContext|null;if(!maybe)return null;const gl:WebGLRenderingContext=maybe,m=build(mode),p=program(gl),pb=arr(gl,m.positions),nb=arr(gl,m.normals),cb=arr(gl,m.colors),ib=gl.createBuffer();if(!ib)throw Error("STADIUM_INDEX_BUFFER_CREATE_FAILED");gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER,ib);const max=m.indices.reduce((a,b)=>Math.max(a,b),0),uint=max>65535;if(uint&&!gl.getExtension("OES_element_index_uint"))throw Error("STADIUM_INDEX_RANGE_UNSUPPORTED");gl.bufferData(gl.ELEMENT_ARRAY_BUFFER,uint?new Uint32Array(m.indices):new Uint16Array(m.indices),gl.STATIC_DRAW);const pos=gl.getAttribLocation(p,"aPosition"),nor=gl.getAttribLocation(p,"aNormal"),col=gl.getAttribLocation(p,"aColor"),uM=gl.getUniformLocation(p,"uMvp"),uC=gl.getUniformLocation(p,"uCamera"),uS=gl.getUniformLocation(p,"uSunDir");if(pos<0||nor<0||col<0||!uM||!uC||!uS)throw Error("STADIUM_SHADER_LOCATION_FAILED");let vw=1,vh=1;gl.enable(gl.DEPTH_TEST);gl.depthFunc(gl.LEQUAL);gl.disable(gl.CULL_FACE);const bind=(b:WebGLBuffer,loc:number)=>{gl.bindBuffer(gl.ARRAY_BUFFER,b);gl.enableVertexAttribArray(loc);gl.vertexAttribPointer(loc,3,gl.FLOAT,false,0,0)};function resize(width:number,height:number,dpr:number){const w=Math.max(1,Math.round(width*Math.min(dpr,2))),h=Math.max(1,Math.round(height*Math.min(dpr,2)));if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h}vw=w;vh=h;gl.viewport(0,0,w,h)}function render(o:number,z0:number){const a=vw/vh,portrait=a<1,z=Math.min(1.14,Math.max(.92,z0)),rad=o*Math.PI/180,r=(portrait?215:170)/z,h=(portrait?92:67)/z,e:Vec3=[Math.sin(rad)*r,h,Math.cos(rad)*r],v=look(e,[0,8,0],[0,1,0]),pr=persp((portrait?57:47)*Math.PI/180,a,.8,520),mvp=mul(pr,v);gl.clearColor(0,0,0,0);gl.clearDepth(1);gl.clear(gl.COLOR_BUFFER_BIT|gl.DEPTH_BUFFER_BIT);gl.useProgram(p);bind(pb,pos);bind(nb,nor);bind(cb,col);gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER,ib);gl.uniformMatrix4fv(uM,false,mvp);gl.uniform3f(uC,e[0],e[1],e[2]);gl.uniform3f(uS,-.22,.94,.26);gl.drawElements(gl.TRIANGLES,m.indices.length,uint?gl.UNSIGNED_INT:gl.UNSIGNED_SHORT,0)}function destroy(){gl.deleteBuffer(pb);gl.deleteBuffer(nb);gl.deleteBuffer(cb);gl.deleteBuffer(ib);gl.deleteProgram(p)}return{triangleCount:m.indices.length/3,resize,render,destroy}}
