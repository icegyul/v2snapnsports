import { describe, expect, it } from "vitest";
import { createPack01Domain } from "../../../../packages/pack01/domain";
import { createPack02FootballLifeDomain } from "../../../../packages/pack02/domain";
import { projectTrainingAttendanceToCareer } from "../../../../packages/integration/pack01to02";
const coach = { actorUserId: "coach", accountState: "ACTIVE" as const, tenantId: "tenant-a", teamIds: ["team-a"], verifiedRoleGrants: [{ role: "COACH" as const, tenantId: "tenant-a", clubId: "club-a", teamIds: ["team-a"], status: "VERIFIED" as const }], guardianRelations: [], consents: [], safeguardingBlocked: false, feature: "CORE" as const, operation: "team:manage" as const };
const player = { actorUserId: "athlete-a", accountType: "PLAYER" as const, accountState: "ACTIVE" as const, tenantId: "tenant-a", teamIds: ["team-a"], athleteId: "athlete-a", verifiedRoleGrants: [], guardianRelations: [], consents: [], safeguardingBlocked: false, feature: "CORE" as const, operation: "athlete:private-read" as const };
describe("PACK 01 → PACK 02 provenance", () => { it("projects final attendance as a verified provenance-backed training milestone without fabricating history", () => { const training=createPack01Domain(), career=createPack02FootballLifeDomain(); const session=training.createTrainingSession(coach,{teamId:"team-a",objective:"패스"}); training.setAttendance(coach,session.id,"athlete-a","PRESENT"); projectTrainingAttendanceToCareer({training,career,coach,player,sessionId:session.id,athleteId:"athlete-a",seasonId:"2026-u17"}); const event=career.getCareerPassport(player,"athlete-a").events[0]; expect(event).toMatchObject({type:"TRAINING_MILESTONE",source:{type:"TRAINING_ATTENDANCE",id:session.id,version:1,verifiedState:"VERIFIED"}}); }); });
