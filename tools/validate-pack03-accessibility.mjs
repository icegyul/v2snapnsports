/* global process, console, document, getComputedStyle, innerWidth */
import assert from "node:assert/strict";
import { mkdir, writeFile } from "node:fs/promises";
import path from "node:path";
import { chromium } from "/Users/fiftyfy14/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs";
const base = process.env.PACK03_URL || "http://127.0.0.1:4175/v2";
const output = path.resolve("docs/implementation/evidence/pack03/accessibility");
const roles = [["COACH", "코치 워크스페이스"], ["TEAM_MANAGER", "팀 매니저 워크스페이스"], ["CLUB_DIRECTOR", "클럽 디렉터 워크스페이스"], ["REFEREE", "심판 워크스페이스"], ["AGENT", "에이전트 워크스페이스"], ["ANALYST", "분석가 워크스페이스"]];
await mkdir(output, { recursive: true }); const browser = await chromium.launch({ headless: true, executablePath: "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome", env: { ...process.env, TMPDIR: "/Volumes/700gb" } });
try { const page = await browser.newPage({ viewport: { width: 390, height: 844 } }); await page.emulateMedia({ reducedMotion: "reduce", forcedColors: "active" }); await page.goto(`${base}/manager`, { waitUntil: "networkidle" }); const failures = [];
  for (const [role, heading] of roles) { await page.getByRole("button", { name: `${role} 역할로 전환` }).click(); await page.getByRole("heading", { name: heading, exact: true }).waitFor(); const result = await page.evaluate(() => { const all = [...document.querySelectorAll("button,a")].filter((element) => { const style = getComputedStyle(element); return style.display !== "none" && style.visibility !== "hidden" && !element.hasAttribute("hidden"); }); return { h1: document.querySelectorAll("h1").length, unnamed: all.filter((element) => !((element.getAttribute("aria-label") || element.textContent || "").trim())).length, small: all.filter((element) => { const box = element.getBoundingClientRect(); return box.width < 44 || box.height < 44; }).length, overflow: document.documentElement.scrollWidth > innerWidth }; }); if (result.h1 !== 1 || result.unnamed || result.small || result.overflow) failures.push(`${role}:${JSON.stringify(result)}`); await page.keyboard.press("Tab"); assert.ok(await page.evaluate(() => ["A", "BUTTON"].includes(document.activeElement?.tagName || "")), `${role} keyboard focus`); }
  await page.screenshot({ path: path.join(output, "forced-colors-reduced-motion.png"), fullPage: true }); assert.deepEqual(failures, []); for (const name of ["keyboard-focus-results.md", "accessible-name-results.md", "touch-target-results.md", "reduced-motion-results.md", "contrast-results.md"]) await writeFile(path.join(output, name), `# PACK 03 accessibility\n\nPASS: ${roles.length}/${roles.length} role workspaces under forced colors and reduced motion.\n`); console.log(`PASS: PACK03 accessibility ${roles.length}/${roles.length} workspaces`);
} finally { await browser.close(); }
